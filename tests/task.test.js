/**
 * 
 * Tests related with Task methods
 * 
 * @requires jest
 * @requires supertest
 * @requires app
 * @requires jwt
 * @requires mongoose
 * 
 */
const supertest = require("supertest");
const Task      = require("../src/models/task");

test('Should create task for user' , async () => {
    
});